# Pinned by pip-compile from requirements.in
# Do not edit manually — run: pip-compile requirements.in --no-emit-hashes -o requirements.lock
#
# Hashes are intentionally omitted: platform-specific wheels (e.g. MarkupSafe,
# numpy, pymeshlab) have different hashes on x86_64 vs aarch64, causing hash
# mismatch failures when building on the non-compile platform.
#
# pythonocc-core is intentionally absent: it has no PyPI wheel and is
# installed via conda in the Dockerfile (see RUN conda install).

blinker==1.9.0
click==8.4.1
Flask==3.1.3
itsdangerous==2.2.0
Jinja2==3.1.6
MarkupSafe==3.0.3
numpy==2.4.6
pymeshlab==2025.7.post1
Werkzeug==3.1.8
